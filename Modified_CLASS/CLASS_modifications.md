# CLASS Modifications Summary

The following files were modified to implement HSMT:

- `source/background.c` → added projection opacity κ(ℓ)
- `source/thermodynamics.c` → added leakage terms
- `source/perturbations.c` → running G_N(ℓ) in metric equations
- `source/bbn/primat/` → partition-blending for lithium suppression

Exact diff files and full patched CLASS will be uploaded in the next update.
